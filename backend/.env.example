PORT=3076
HOST=0.0.0.0
# Run API only when frontend is hosted separately (recommended for staging/prod)
API_ONLY=1
NODE_ENV=production

# Use "memory" for local smoke tests without MongoDB
# MONGODB_URI=memory
MONGODB_URI=mongodb://127.0.0.1:27017/diagnostic_booking

JWT_SECRET=change_me_in_production

# Public frontend origin
CLIENT_URL=http://207.180.213.153:5075
# Extra allowed browser origins (comma-separated)
CLIENT_URLS=http://207.180.213.153:5075,http://207.180.213.153:3075,http://localhost:5173,http://localhost:3000
